<% unless current_user.current_user? user %>
  <div id="follow_form">
    <% if current_user.following? user %>
      <%= render "unfollow", user: user, relationship: relationship %>
    <% else %>
      <%= render "follow", user: user, relationship: relationship %>
    <% end %>
  </div>
<% end %>
